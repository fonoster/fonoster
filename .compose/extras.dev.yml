version: '3.8'

services:

  tts:
    ports:
      - ${TTS_ENGINE_PORT}:${TTS_ENGINE_PORT}
  logsdriver:
    ports:
      - ${LOGS_DRIVER_PORT}:${LOGS_DRIVER_PORT}
  logsmonitor:
    ports:
      - ${LOGS_MONITOR_PORT}:${LOGS_MONITOR_PORT}

