var s = require("./lib/swagger");
          
s.authorizations.add("api_key", new s.ApiKeyAuthorization("api_key", "694d4d1e3abf0e18500020fbb5202a40acc82748efd7fdc73", "header"));
 
wordnik = new s.SwaggerApi({
             url: "https://api.wordnik.com/v4/resources.json",
             success: function ()
             {console.log(s.apis);
               if(s.ready === true) {
               args = { word: 'sanctimonious' }
               s.word.getDefinitions(args, function (definitions) {
                   for (var i = 0; i < definitions.length; i++) {
                      var definition = definitions[i];
                      console.log(definition.partOfSpeech + ": " + definition.text);
                   }
                });
               }
else {
console.log("not ready!");
}
             }
          });
